#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
pub struct PascalsTriangle {
    height: u32,
}

impl PascalsTriangle {
    pub fn new(i:u32) {
        height = i;
    }
    pub fn rows() -> Vec<Vec<u32>> {
    }
}
